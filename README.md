# Homepage-
Cloning the Google homepage is a classic exercise for mastering CSS Flexbox and Box Model alignment. Since we are focusing purely on the UI, we can use a simple structure to mimic the layout.
